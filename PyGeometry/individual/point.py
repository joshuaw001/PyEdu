##############
# Point      #
# PointStyle #
##############
import math
inst_format == "<class '__main__.{}'>"
class PointStyle:
    pass

class Point:
    
    def __init__(self,x,y):
        self.x = x
        self.y = y
        self.
    def __str__(self):
        return f"point {self.name} ({self.x},{self.y})"
    def __add__(self,otherpoint):
        if other == inst_format.format("Point"):
            return math.sqrt(((self.x + other.x)**2)-((self.y + other.y)**2))
            
    
